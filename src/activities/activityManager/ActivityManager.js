import React from 'react';
import "./ActivityManager.css";

export default function ActivityManager({ children }) {
    return (
        <main>
            {children}
        </main>
    )
}
